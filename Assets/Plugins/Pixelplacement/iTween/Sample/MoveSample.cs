using UnityEngine;
using System.Collections;

public class MoveSample : MonoBehaviour
{	
	void Start()
	{
		//float rand = Random.Range(-0.1f,-2.0f);
		//iTween.MoveBy(gameObject, iTween.Hash("y", rand, "easeType", iTween.EaseType.easeInOutSine, "loopType", "pingPong", "delay", .1, "time",9));
	}
}

